using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace JabbR_Core.Configuration
{
    public class DeleteMeTest
    {
        public bool IsPrime(int n)
        {
            if(n < 2)
            {
                return false;
            }
            else
            {
                // It's prime because I say so
                return true;
            }
        }
    }
}
